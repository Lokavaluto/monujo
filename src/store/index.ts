/* eslint-disable */

///<reference types="@types/node"/>

import { createStore } from 'vuex'


export default createStore({
  state: {
    operationSelector:0,
    showCredit:false
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
  getters: {
  }
})
